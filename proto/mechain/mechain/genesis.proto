syntax = "proto3";
package mechain.mechain;

import "gogoproto/gogo.proto";
import "mechain/mechain/params.proto";

option go_package = "me-chain/x/mechain/types";

// GenesisState defines the mechain module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
}
